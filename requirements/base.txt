# ==========================================================================
# OdontoSaaS — Dependências BASE (comuns a todos os ambientes)
# Fixadas (pinned) para builds reprodutíveis.
# ==========================================================================

# --- Core / Framework ---
Django==5.1.4
djangorestframework==3.15.2

# --- Multi-tenant (schema-per-tenant) ---
django-tenants==3.7.0

# --- Banco de dados (PostgreSQL 16) ---
psycopg[binary]==3.2.3

# --- Processamento assíncrono / agendamento ---
celery==5.4.0
redis==5.2.1
django-celery-beat==2.7.0          # agendamentos persistidos no banco (antecedência por clínica)

# --- Integração Google Calendar (OAuth2) ---
google-auth==2.37.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.156.0

# --- Cliente HTTP (WAHA / WhatsApp) ---
requests==2.32.3

# --- Segurança de dados sensíveis (tokens Google criptografados) ---
cryptography==44.0.0

# --- Configuração via variáveis de ambiente ---
django-environ==0.11.2

# --- Servidor de aplicação (WSGI) — usado em dev (compose) e produção ---
gunicorn==23.0.0
