# Stack bleeding-edge (React 19, TS 6, Vite 8): vários pacotes ainda fixam peers
# em versões anteriores (ex.: openapi-typescript peer typescript ^5). Relaxamos a
# resolução de peers para instalar de forma consistente (dev e CI).
legacy-peer-deps=true
